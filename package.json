{
  "name": "toshi22-widget-quickstart",
  "private": true,
  "workspaces": [
    "frontend",
    "node"
  ],
  "scripts": {
    "start": "npm run start --prefix frontend",
    "install-all": "npm install --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
