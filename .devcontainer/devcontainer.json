{
  "name": "Toshi22 Widget App",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "npm install --prefix frontend && npm install --prefix api && npm install -g azure-functions-core-tools@4 --unsafe-perm true"
}

